@model webapi.Models.User

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    List<webapi.Models.User> list = ViewBag.List;
}

<h2>Brugere</h2>


<table>
    <tr>
        <th>Navn</th>
        <th>Email</th>
    </tr>
    @foreach (var item in list)
    {
        <tr>
            <td>@item.Firstname @item.Surname</td>
            <td>@item.Email</td>
        </tr>
    }
</table>

